apiVersion: v1
kind: LimitRange
metadata:
  name: cpu-limit-range
spec:
  limits:
  - default:
      memory: 256Mi
      cpu: 100m
    defaultRequest:
      cpu: 200m
      memory: 128Mi
    type: Container
